{
  "extends": "./tsconfig.json",
  "comment": "scripts/ is excluded so the app's build root stays src/ (output at dist/main.js, not dist/src/main.js). The break-glass CLI in scripts/ is run with ts-node, never compiled here.",
  "exclude": ["node_modules", "test", "dist", "prisma", "scripts", "**/*spec.ts"]
}
